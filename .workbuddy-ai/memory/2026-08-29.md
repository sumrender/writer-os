# 2026-08-29

## Issue #13 — Rename Story Bible state → Story Facts (completed)
- Continued an `/implement` handoff: behavior-preserving expand–contract rename of the fact-store types across `benchmarks` + `ui`.
- Verified: `pnpm typecheck` green; `pnpm test` green (357 tests, no behavior change).
- Fixed doc drift in `benchmarks/README.md` (lines ~70–74, ~143: `bibleSoFar`→`factsSoFar`, `bibleStateAsOf`→`factsAsOf`, "bible state/entries"→facts, `lib/bible-merge.ts`→`lib/fact-merge.ts`) and aligned `assertion-match.ts` comments to "fact(s)". Did NOT touch domain-sense "Story Bible" in docs/PRD.md, docs/TESTING.md, docs/adr/*, CONTEXT.md, README.md.
- Manual code review (no `code-review` skill available) on Standards + Spec axes: no dual-form exports / shims; events.ts re-exports only `StoryFacts`/`parseStoryFacts`; wire field `bible`→`facts` consistent on emit + parse; intentional aliases `StoryFactsState`/`StoryFactsViewer` present.
- Committed to `main` as `6088520` (git detected 4 renames). Commit records BREAKING CHANGE: old persisted `ui-runs` JSON under `bible` field names no longer parse (accepted for dev surface, ADR-0006).
- Not pushed (left for user). `dist/` and `results/` are gitignored; stale `bible` leftovers remain in dist only.
